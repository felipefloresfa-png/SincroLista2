rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ABRIMOS TEMPORALMENTE PARA DIAGNÓSTICO
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
