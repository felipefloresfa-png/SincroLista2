{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "Perfil de usuario sincronizado",
      "type": "object",
      "properties": {
        "uid": { "type": "string" },
        "displayName": { "type": "string" },
        "familyId": { "type": "string" },
        "photoURL": { "type": "string" }
      },
      "required": ["uid", "displayName", "familyId"]
    },
    "ShoppingList": {
      "title": "ShoppingList",
      "description": "Una lista de la compra grupal",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "familyId": { "type": "string" },
        "color": { "type": "string" },
        "isArchived": { "type": "boolean" }
      },
      "required": ["name", "familyId"]
    },
    "GroceryItem": {
      "title": "GroceryItem",
      "description": "Un producto en la lista",
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "listId": { "type": "string" },
        "familyId": { "type": "string" },
        "checked": { "type": "boolean" },
        "quantity": { "type": "string" },
        "is_carryover": { "type": "boolean" }
      },
      "required": ["name", "listId", "familyId"]
    },
    "PurchaseHistory": {
      "title": "PurchaseHistory",
      "description": "Un registro de compra finalizada",
      "type": "object",
      "properties": {
        "items": { "type": "array" },
        "familyId": { "type": "string" },
        "listId": { "type": "string" },
        "timestamp": { "type": "string" },
        "totalItems": { "type": "number" },
        "totalCost": { "type": "number" },
        "storeName": { "type": "string" }
      },
      "required": ["items", "familyId", "timestamp"]
    }
  },
  "firestore": {
    "/users/{userId}": { "schema": "UserProfile", "description": "Perfiles de usuario" },
    "/lists/{listId}": { "schema": "ShoppingList", "description": "Listas de la compra" },
    "/items/{itemId}": { "schema": "GroceryItem", "description": "Productos de las listas" },
    "/history/{historyId}": { "schema": "PurchaseHistory", "description": "Historial de compras finalizadas" }
  }
}
